{% extends 'base.html.twig' %}
{% block title %}Crea una nueva marca{% endblock %}
{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-6">
                <h2>Añade una nueva marca</h2>
                {{ form_start(form) }}
                <span class="input-group-btn">
                            {{ form_row(form.nombre, { 'attr': {'class': 'form-control'} }) }}
                </span>
                {{ form_row(form.activo, {'attr':{'class':'form-check-input'}})}}
                {{ form_row(form.descripcion,{ 'attr': {'class': 'form-control'} } )}}
                {{ form_row(form.Crear, { 'attr': {'class': 'btn btn-primary'} })}}

                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}